{% extends 'partials/base.html.twig' %}

{% block content %}
  {% include 'partials/postcontent.html.twig' %}

  <div class="portfolio-list">
    {% for child in page.collection if not child.header.item_small %}
      {% include 'partials/portfolio-item.html.twig' with {'page':child} %}
    {% endfor %}
  </div>

  <div class="portfolio-list-more">
    <h2>Jag har även jobbat med</h2>
    {% for child in page.collection if child.header.item_small %}
      {% include 'partials/portfolio-item-small.html.twig' with {'page':child} %}
    {% endfor %}
  </div>
{% endblock %}
